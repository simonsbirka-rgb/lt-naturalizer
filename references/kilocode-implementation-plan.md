# KiloCode Implementation Plan — lt-naturalizer

> **Generated by:** KiloCode (Plan Mode)
> **Date:** 2026-04-03
> **Status:** Currently running locally

## Summary
Plan is ready for implementation. The plan outlines the complete transformation strategy with 4 implementation phases covering vocabulary swap, syntax refactor, new logic addition, and test updates while preserving all CLI architecture constraints.

---

## Discoveries
- The engine uses a fixed 70% pattern / 30% statistical weight formula for composite scoring. All new Lithuanian metrics must respect this weight split.
- All pattern detectors follow an exact standard return schema. Deviating from this schema will cause silent failures in the analyzer pipeline.
- An existing `findMatches()` helper correctly handles line/column numbering and offset calculation. All new detectors must use this helper.
- Lithuanian reference rules are fully documented in the research spec file, no external data is required.

## Relevant Files
| File | Notes |
|---|---|
| `/src/vocabulary.js` | Preserve existing export names exactly, only replace array contents |
| `/src/patterns.js` | Keep all helper functions, registry infrastructure, only replace pattern definitions |
| `/src/analyzer.js` | Inject new calculation functions into existing pipeline, do not modify output structure |
| `/references/lithuanian-ai-patterns.md` | Official source of truth for all linguistic rules |
| `/src/cli.js` | ABSOLUTELY NO MODIFICATIONS PERMITTED |

## Implementation Notes
- All existing function signatures, exports, and JSON output keys must remain byte identical. Only internal logic may be changed.
- The native `wordRegex()` helper correctly handles Lithuanian diacritics, do not write custom regex matching.
- Add new nominalization/genitive metrics at 15% total weight to preserve the existing 70/30 scoring balance.
- Run tests after each file modification - the existing test suite will catch breakage to the public interface.
